[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
markers =
    unit: Unit tests (fast, no external deps)
    integration: Integration tests (may need files)
    e2e: End-to-end pipeline tests
    slow: Tests that take >5s
addopts = -v --tb=short
